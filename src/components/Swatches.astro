<div class="swatches">
  <slot />
</div>

<style>
  .swatches {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    background: var(--bg-primary);
    padding: 22px;
    border-radius: 22px;
  }
</style>
